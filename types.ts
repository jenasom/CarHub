export interface AnimationProps {
  className?: string;
  delay?: number;
}

export interface StatItem {
  id: number;
  label: string;
  value: string;
  sub: string;
}

export interface NavLink {
  name: string;
  href: string;
}